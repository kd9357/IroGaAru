using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PurpleBoss : Enemy {
    //Should we be inheriting, or is a boss different enough to merit a separate class?
    //If the former, need to change most private vars and some functions to protected virtual in enemy.cs

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

}
